# ---------------------------------------------------------------------------
# OASIS-3 data is credentialed and must NEVER be committed to this repository.
# ---------------------------------------------------------------------------
*.mgz
*.nii
*.nii.gz

oasis3_radiomics_smoketest/freesurfer/
oasis3_radiomics_smoketest/prepared_nifti/
oasis3_radiomics_smoketest/_oasis_scripts/

oasis3_data/
oasis3_freesurfer_catalog.csv

# --- clinical linkage outputs (regenerate with `python cli.py clinical-link`) ---
clinical_results/
dataset/
prepared_nifti/
*.log

# --- python ---------------------------------------------------------------
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/
.coverage
oasis3_pilot/
results_pilot/

# --- credentials: NEVER commit NITRC/OASIS session state ------------------
.cookies-*.txt
.DS_Store

# --- supervised label outputs (regenerate with `cli.py supervised-labels`) -
supervised_dataset/

# ===========================================================================
# CREDENTIALED OASIS-3 DATA AND PER-PARTICIPANT DERIVATIVES
# ===========================================================================
# OASIS-3 is credentialed and must not be redistributed. This covers the raw
# clinical forms, the MRI session catalogue (per-participant demographics) and
# every per-participant derivative. Obtain them through the authorised
# OASIS/NITRC workflow and regenerate the derivatives with the CLI.
diagnostic/
results_full/
dataset_full/
acquisition/
supervised_dataset/
clinical_results/
oasis3_mri_catalog.csv
luanmantegazine_*.csv
